# Server
PORT=3000
NODE_ENV=production

# Session & CSRF secrets (use long random strings in production)
SESSION_SECRET=replace-with-a-strong-random-secret
CSRF_SECRET=replace-with-another-strong-random-secret

# Database
DATABASE_PATH=./translator_finder.db
